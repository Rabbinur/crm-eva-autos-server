services:
  zsi-restaurant-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5005:5005"
    env_file:
      - .env
    networks:
      - zsi-restaurant-server-network
networks:
  zsi-restaurant-server-network:
    driver: bridge
